# Context Engine v2 — Application config
# Index names (prefix with org_id for multi-tenant: {org_id}-feedback)
indexes:
  feedback: feedback
  customers: customers
  product_context: product-context

app:
  name: Context Engine v2
  debug: false
  cors_origins: ["*"]
